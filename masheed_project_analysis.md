# تحليل مشروع مشيد (Masheed ERP)

تم إجراء تحليل شامل للمشروع بناءً على الكود الحالي، مخطط قاعدة البيانات (Schema)، والواجهات الأمامية.

## 1. المشاكل الحالية ونقاط الضعف

### أ. قاعدة البيانات ونموذج البيانات (Database)
*   **أنواع الأوقاف:** النظام الحالي يفترض أن كل الأوقاف متماثلة أو يركز فقط على "الوقف العقاري". لا يوجد تصنيف (خيري، أهلي/ذري، مشترك).
*   **نظام التوكينيزيشن (Tokenization):** غائب تماماً. لا توجد طريقة لتقسيم الوقف إلى أسهم أو حصص (Shares) وتوزيعها على المستفيدين.
*   **البيانات الوهمية:** النظام لا يحتوي على بيانات حقيقية للتجربة بشكل صحيح، ولا توجد آلية لإدخال بيانات واقعية بسلاسة من الواجهة.

### ب. لوحة التحكم والواجهة الأمامية (Dashboard & Frontend)
*   **الربط مع الاستيثاق (Authentication):** لوحة التحكم (Dashboard) معزولة تماماً عن نظام الدخول (`Supabase Auth`). اسم المستخدم ثابت في الكود ("فيصل المطيري")، ولا يوجد زر لتسجيل الخروج، ولا يتم التحقق مما إذا كان المستخدم مسجلاً للدخول فعلياً قبل عرض البيانات.
*   **البيانات الثابتة (Hardcoded Data):** الإحصائيات، الأنشطة، الإيرادات، والمصروفات كلها ثابتة في ملف `dashboard/page.tsx` ولا تعكس ما هو موجود في قاعدة البيانات.
*   **إدارة الأوقاف:** القائمة المنسدلة لاختيار الوقف في الشريط الجانبي (Sidebar) ثابتة (Waqfs Array)، ولا تجلب الأوقاف الخاصة بالمستخدم من جدول `waqfs`.

### ج. الصفحة الرئيسية (Landing Page)
*   الصفحة الرئيسية تعتبر ضعيفة (Weak). تحتاج إلى تحسين في التصميم (UI/UX) لتبدو كبرنامج مؤسسي احترافي (SaaS ERP).
*   تفتقر إلى التفاعلية (Micro-animations)، والتوضيح الفعلي لمميزات "التوكينيزيشن" والحوكمة.

---

## 2. ميزات نظام الـ ERP (التوكينيزيشن وإدارة المستفيدين)

تم تصميم الهيكلية الجديدة لتدعم نظام **Tokenization** لتقسيم الوقف:
1.  **رأس مال الوقف (Total Shares):** كل وقف سيكون له `total_shares` (مثلاً 10,000 سهم أو 100%).
2.  **أسهم المستفيدين (Beneficiary Shares):** كل مستفيد سيملك `shares_owned`.
3.  **نظام التوزيع الآلي (Automated Distributions):**
    *   عند دخول إيراد (Income Transaction)، يمكن لمدير الوقف إنشاء "عملية توزيع" (Distribution).
    *   يقوم النظام بعملية حسابية آلية (ERP Logic) لمعرفة نصيب كل مستفيد بناءً على حصته التوكينيزيشن.
    *   تسجيل المدفوعات في جداول خاصة (`distribution_payouts`) ورفع رصيد المستفيد (`wallet_balance`).

---

## 3. التعديلات المطلوبة (تم كتابتها في ملف `02_update_schema.sql`)
*   إضافة `waqf_type` لجدول `waqfs`.
*   إضافة `total_shares` لجدول `waqfs`.
*   إضافة `shares_owned`, `national_id`, `wallet_balance` للمستفيدين.
*   إنشاء جدول `distributions` وجدول `distribution_payouts` لإدارة التوزيعات المالية بناءً على الأسهم.
*   إنشاء دالة `calculate_distribution()` لتقوم بالحساب الآلي للأنصبة.
