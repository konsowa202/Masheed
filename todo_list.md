# قائمة المهام الشاملة (To-Do List)

- [ ] **قاعدة البيانات (Database):**
  - [ ] تنفيذ أمر الـ SQL (`02_update_schema.sql`) في Supabase لإنشاء جداول `distributions` وتحديثات `waqfs` و `beneficiaries`.
  - [ ] التحقق من صلاحيات RLS للجداول الجديدة للتأكد من أمن البيانات.

- [ ] **المصادقة والتسجيل (Authentication):**
  - [ ] برمجة `LoginForm` لربطها فعلياً مع `supabase.auth.signInWithPassword`.
  - [ ] برمجة `RegisterForm` مع إتاحة إدخال "اسم الوقف" وتسجيله آلياً للمستخدم عبر الـ Trigger.
  - [ ] تأمين مسار الدخول (Protected Routes) بلوحة التحكم `Middleware`.
  - [ ] إضافة زر تسجيل الخروج (Sign Out) وتفعيل وظيفته.

- [ ] **لوحة التحكم (Dashboard):**
  - [ ] إزالة البيانات الوهمية (Hardcoded) من `dashboard/page.tsx`.
  - [ ] استبدال اسم "فيصل المطيري" باسم المستخدم الفعلي من قاعدة البيانات (Profile).
  - [ ] جلب قائمة الأوقاف التي يملكها/يديرها المستخدم في القائمة الجانبية (Sidebar).
  - [ ] جلب الإحصائيات والأرقام (Assets, Income, Distributions) من قاعدة البيانات مباشرة.
  - [ ] عرض آخر العمليات من جدول `audit_logs`.

- [ ] **إدارة الأوقاف والمستفيدين (ERP & Tokenization):**
  - [ ] بناء صفحة المستفيدين لعرض الأسهم وإدارتها.
  - [ ] إضافة نموذج لتحديد عدد أسهم الوقف الإجمالية (`total_shares`) عند إنشاء أو تعديل إعدادات الوقف.
  - [ ] بناء واجهة "إنشاء توزيع مالي" بحيث يقوم النظام بحساب نصيب كل مستفيد بناءً على حصته.

- [ ] **الصفحة الرئيسية (Landing Page):**
  - [ ] تحسين النصوص (Copywriting) لتعكس وظيفة الـ ERP وإدارة الأوقاف والتوكينيزيشن.
  - [ ] إضافة مؤثرات بصرية وتصحيح المسافات والمحاذاة في قسم المميزات والشركاء.
  - [ ] التأكد من أن جميع الأزرار في الصفحة الرئيسية (Login, Start now) تعمل وتحول لصفحاتها الصحيحة.

- [ ] **الامتثال والتقارير:**
  - [ ] إضافة خيار تصدير التقارير لتكون جاهزة للربط مستقبلاً مع ZATCA و GAW.
